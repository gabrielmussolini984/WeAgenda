<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="/">We Agenda</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      {{#if user}}
      <li class="nav-item">
        <a class="btn btn-success btn-sm nav-link text-white mr-2" href="/login">Painel Administrativo</a>
      </li>
      {{/if}}
      <li class="nav-item">
        {{#if user}}
        <a class="btn btn-danger btn-sm nav-link text-white" href="/login/sair">Sair</a>
        {{else}}
        <a class="btn btn-success btn-sm nav-link text-white" href="/login">Entrar / Registrar</a>
        {{/if}}
      </li>
    </ul>
  </div>
</nav>